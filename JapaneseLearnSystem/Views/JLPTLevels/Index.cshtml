@model IEnumerable<JapaneseLearnSystem.Models.JLPTLevel>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.JLPTLevelName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Description)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.JLPTLevelName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Description)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.JLPTLevelID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.JLPTLevelID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.JLPTLevelID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
