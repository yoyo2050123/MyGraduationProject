@model IEnumerable<JapaneseLearnSystem.Models.JPLTLevel>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.JPLTLevelName)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.JPLTLevelName)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.JPLTLevelID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.JPLTLevelID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.JPLTLevelID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
